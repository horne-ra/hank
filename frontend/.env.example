# Copy to .env.local and fill in. TOKEN_AUTH_SECRET must match project-root .env
# (same value the FastAPI token server uses for Bearer auth on POST /token).

BACKEND_URL=http://localhost:8000
TOKEN_AUTH_SECRET=
